services:
  app:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data  # dossier local 'data' est monté dans '/app/data'
    environment:
     - DATA_PATH=/app/data/processed/global_housing_processed.csv